import { FiltroTerminadoPipe } from './filtro-terminado.pipe';

describe('FiltroTerminadoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroTerminadoPipe();
    expect(pipe).toBeTruthy();
  });
});
